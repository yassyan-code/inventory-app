-- 004_add_team_id_draft.sql
-- マルチテナント化（チーム/店舗単位のデータ分離）に向けた準備メモ
--
-- 目的: 現状は「ログイン済みユーザーなら誰でも全データを読み書き可能」という
--       簡易ポリシーになっている。これをteam_id単位で分離する変更の下書き。
--
-- 今後の作業（このファイルはまだ実行しない・計画メモ）:
--   1. teams テーブルを新設（id, name, created_at）
--   2. products テーブルに team_id 列を追加（FK -> teams）
--   3. 既存データにデフォルトチームを割り当てるマイグレーション
--   4. RLSポリシーを「自分のteam_idの行のみ読み書き可」に変更
--   5. 認証ユーザーとチームの紐付けテーブル(team_members)を追加
--
-- 実行はまだ行わない。設計が固まり次第、実SQLに書き換えて
-- 002/003と同様にユーザーがSupabase SQL Editorで実行する。
